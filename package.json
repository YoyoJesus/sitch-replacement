{
  "name": "bun-elysia-vc",
  "type": "module",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:contact": "bun build src/contact.ts --outfile=src/contact.js --target=browser",
    "dev": "bun run build:contact && bun run --watch src/index.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
